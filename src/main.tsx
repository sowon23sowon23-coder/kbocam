import React from "react";
import ReactDOM from "react-dom/client";
import KboEventBanner from "../KboEventBanner";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <KboEventBanner />
  </React.StrictMode>
);
